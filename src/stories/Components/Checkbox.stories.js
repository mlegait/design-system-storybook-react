import { storiesOf } from '@storybook/react';
import React from 'react';

storiesOf('Components|Form/Checkbox', module).add(
  'default',
  () => <div>To replace with a checkbox</div>,
  { position: 410 }
);
